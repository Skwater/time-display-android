/.gradle/
/build/
/app/build/
/.idea/
*.iml
/local.properties
/.tooling/
/.gradle-user-home/
/app/signing/*.keystore
# 签名凭据（storeFile/storePassword/keyAlias/keyPassword），仅本地保留，绝不上传
/keystore.properties

# 工作区规范（2026-09）：本地保留，不上传
# 文档仅上传 README.md，其余文档保留在本地 docs/（不纳入版本控制）
/docs/
# 临时文件与回收站：与源码无关的文件一律移入对应目录，禁止删除
/.tmp/
/.trash/
# 版本化 APK 仅保留在本地 releases/，不上传 GitHub（节省仓库空间）
/releases/
